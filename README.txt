Installations

pip install pygame matplotlib ipython
pytorch on terminal
pip3 install torch torchvision

How to run
Run the MADDPG file to train the agent
you may choose to use baselines among:
BASELINE =
#'' : no baseline (MADDPG agent)
# 'oneDir' : moves one direction
#'random' : random movement

*Note: for matplotlib to work properly, we recommend using pycharm interpreter